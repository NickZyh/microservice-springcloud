统一依赖管理项目
主要用于对微服务依赖统一版本管理;要注意的是,由于我们是单独创建的文件夹,所以即使创建了一个
pom.xml文件,但是此时该pom还未被监控到,因为只是相当于在一个普通文件夹中创建了一个普通的xml文件,所以我们应该让idea
监控该pom文件,步骤如下：
   1 选择IDEA右边的MAVEN选项
   2 点击里面的 + 号
   3 找寻到刚刚创建的pom.xml,点击进行监控;

在实际开发中，我们所有的项目都会依赖这个 dependencies 项目，整个项目周期中的所有第三方依赖的版本也都由该项目进行管理。